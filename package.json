{
  "name": "fastify-ts-cloudrun-template",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build/; npx tsc -p tsconfig.json",
    "start": "node build/index.js",
    "develop": "rm -rf build/ && concurrently \"npx tsc -p tsconfig.json --watch\" \"nodemon -q build/index.js\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/autoload": "^5.7.1",
    "fastify": "^4.23.2",
    "fastify-plugin": "^4.5.1"
  },
  "devDependencies": {
    "@types/node": "^20.8.2",
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1",
    "typescript": "^5.2.2"
  }
}